{
    app = {

        scraperClientId = 1

        fileSys = {

            baseDirWIN = "D:\\RealEstateApp\\Apps\\WebScrapers"
            baseDirMAC = "/Users/janis/Projects_Data/ProjectRET/Apps/WebScrapers"

            chromeDriverPath = "chromedriver.exe"
            chromeDriverPathMAC = "chromedriver"

            jsonsDir  = "Data"
            imagesDir = "Images"
            websegmentsDir = "Websegments"
            websegmentBeingProcessedFileName = "processedWebsegment.txt"
        }

    }

     chromeDriver = {

        # TODO move somewhere else + put to application.conf
        isMac = true

        headlessDriver = false
        checkForIdleDriverInterval = 600 seconds
        maxIdleDriverInterval = 1 minute
        maxIntervalSinceLastDriverRestart = 30 minutes
     }

     scraping = {

        imageScrapingEnabled = false

        scrapersSettings = [
            {website: "SREALITY", dataType: "INZERAT", minScrapersCount: 2, maxScrapersCount: 5, lowerItemScrapingDurationMillis = 1500, upperItemScrapingDurationMillis = 10000}
            {website: "SREALITY", dataType: "IMAGES", minScrapersCount: 2, maxScrapersCount: 4, lowerItemScrapingDurationMillis = 800, upperItemScrapingDurationMillis = 5000}
            {website: "BEZ_REALITKY", dataType: "INZERAT", minScrapersCount: 1, maxScrapersCount: 1, lowerItemScrapingDurationMillis = 1500, upperItemScrapingDurationMillis = 6000}
            {website: "BEZ_REALITKY", dataType: "IMAGES", minScrapersCount: 1, maxScrapersCount: 1, lowerItemScrapingDurationMillis = 1500, upperItemScrapingDurationMillis = 6000}
            {website: "IDNES_REALITY", dataType: "INZERAT", minScrapersCount: 1, maxScrapersCount: 1, lowerItemScrapingDurationMillis = 1500, upperItemScrapingDurationMillis = 6000}
            {website: "IDNES_REALITY", dataType: "IMAGES", minScrapersCount: 1, maxScrapersCount: 2, lowerItemScrapingDurationMillis = 1500, upperItemScrapingDurationMillis = 6000}
        ]

        websResponsiveness = {
            minTotalScrapedItemsCountToConsider = 15
            maxStatsAge = 5 minutes
            responsivenessCheckInterval = 20 seconds
        }

        websegments = {
            lastProcessedSegmentsAgeToConsider = 24 hours
        }

        cycles = {
            calculateNextCycleStart = false
            # relevant for calculateNextCycleStart = false
            # TODO this does not seem to work ...
            startNewCycleDelay = 10 hours
        }

        ipc = {
           dontSendNewDataWhileUnsentFilesExist = true
        }

     }
}